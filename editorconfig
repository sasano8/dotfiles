# 個人グローバル EditorConfig（~/.editorconfig へ symlink）。
# Makefile はレシピ行の先頭がタブ必須（space だと動かない）ため、これだけは規定する。
# Python など他の言語はフォーマッタ（ruff 等）に任せ、ここでは規定しない（衝突回避）。
root = true

[{Makefile,*.mk}]
indent_style = tab
